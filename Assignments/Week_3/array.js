var numbers = [1,5,8,9,3];
var large = 0;

//function max(numbers) {

    for (i=0; i <= large; i++) {
        if (numbers[i]>large) {
            var large=numbers[i];
            
        }
        
    }

    console.log(large);
    
//}